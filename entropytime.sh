#!/bin/sh
#this has to be a large file, I use a different one but did not get clearance to post it, so I found another one that is probably big enough. If this isnt large enough you should probably invest in a hardware RNG.
wget -o /dev/null -O /dev/null https://speed.hetzner.de/10GB.bin
